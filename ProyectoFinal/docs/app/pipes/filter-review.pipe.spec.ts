import { FilterReviewPipe } from './filter-review.pipe';

describe('FilterReviewPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReviewPipe();
    expect(pipe).toBeTruthy();
  });
});
